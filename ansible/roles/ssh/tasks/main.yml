---
- name: Deploy sshd_config from template
  template:
    src: sshd_config.j2
    dest: /etc/ssh/sshd_config
    owner: root
    group: root
    mode: '0600'
    validate: sshd -t -f %s

- name: Restart SSH
  systemd:
    name: ssh
    state: restarted
